pandit = [4, 1, 7, 11, 12, 13, 17,15,19];
//mapping 

arr = pandit.map(
  (double = (x) => 
     x * 2
  )
);
console.log(arr);
prr = pandit.map(
  (square = (x) => 
     x * x
  )
);
console.log(prr);
drr = pandit.map(
  (triple = (x) => 
     x * 3 
  )
);
console.log(drr);
mrr = pandit.map(
  (cube = (x) => 
     x * x * x
  )
);
console.log(mrr);
