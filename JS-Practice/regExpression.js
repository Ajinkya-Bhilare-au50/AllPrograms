// const re = /^(a|e|i|o|u).*(\1)$/;

// const re = /^([aeiou]).+\1$/;
// 		/^ start of regex
// 		() shows list of characters used for matching
// 		. matches any ch.
// 		+ concatanate two strings of characters.
// 		1 matches if existed.
// 		$/ used for ending
X = 41;
console.log(Math.ceil(X / 10) * 10); // for rounding to nearest tenth

